# Local app dev server (nginx-proxied) — production Vercel doesn't have the new
# workout routes until we deploy. .env.production still points at Vercel for builds.
REACT_APP_API_NEW=https://app.gymnasticbodies.dev